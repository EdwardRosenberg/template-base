name: CI

on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

permissions:
  contents: read

jobs:
  backend:
    name: Backend Build & Test
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: true
      matrix:
        os: [ubuntu-latest]
        # Add additional OS versions if needed:
        # os: [ubuntu-latest, macos-latest, windows-latest]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Placeholder: Add backend-specific setup steps here
      # Example for Java/Maven:
      # - name: Set up JDK
      #   uses: actions/setup-java@v4
      #   with:
      #     java-version: '25'
      #     distribution: 'temurin'
      #     cache: 'maven'
      #
      # - name: Build with Maven
      #   run: mvn clean install
      #
      # - name: Run tests
      #   run: mvn test

      # Example for Python:
      # - name: Set up Python
      #   uses: actions/setup-python@v5
      #   with:
      #     python-version: '3.11'
      #     cache: 'pip'
      #
      # - name: Install dependencies
      #   run: |
      #     python -m pip install --upgrade pip
      #     pip install -r requirements.txt
      #
      # - name: Run tests
      #   run: pytest

      - name: Placeholder - Backend build steps
        run: echo "Add backend build and test steps here"

  frontend:
    name: Frontend Build & Test
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: true
      matrix:
        os: [ubuntu-latest]
        # node-version: [18.x, 20.x]  # Uncomment to test multiple Node versions

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # Placeholder: Add frontend-specific setup steps here
      # Example for Node.js/npm:
      # - name: Set up Node.js
      #   uses: actions/setup-node@v4
      #   with:
      #     node-version: '20'
      #     cache: 'npm'
      #
      # - name: Install dependencies
      #   run: npm ci
      #
      # - name: Run linting
      #   run: npm run lint
      #
      # - name: Build
      #   run: npm run build
      #
      # - name: Run tests
      #   run: npm test

      - name: Placeholder - Frontend build steps
        run: echo "Add frontend build and test steps here"

  # Optional: Summary job that depends on all other jobs (can be removed if not using branch protection rules)
  # This is useful for branch protection rules
  ci-success:
    name: CI Success
    needs: [backend, frontend]
    runs-on: ubuntu-latest
    if: always()
    steps:
      - name: Check job results
        run: |
          backend_result="${{ needs.backend.result }}"
          frontend_result="${{ needs.frontend.result }}"
          if [ "$backend_result" = "failure" ] || [ "$backend_result" = "cancelled" ] || \
             [ "$frontend_result" = "failure" ] || [ "$frontend_result" = "cancelled" ]; then
            echo "One or more CI jobs failed"
            exit 1
          fi
          echo "All CI jobs passed successfully"
